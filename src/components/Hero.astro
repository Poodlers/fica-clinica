---
import { clinic } from "../data/clinic";

const whatsappText = encodeURIComponent(
  "Olá! Gostaria de marcar uma consulta."
);
const whatsappLink = `https://wa.me/${clinic.phoneE164.replace("+", "")}?text=${whatsappText}`;
---

<header class="pt-10 pb-6">
  <div class="container">
    <nav class="flex items-center justify-between gap-4 flex-wrap">
      <div class="flex items-center gap-3">
        <div class="card-soft w-11 h-11 rounded-2xl grid place-items-center">
          <span class="font-extrabold text-brand-800"
            ><img
              src="fica_logo.jpeg"
              alt="Logo"
              class="w-full h-full object-contain"
            /></span
          >
        </div>
        <div>
          <div class="font-extrabold text-brand-900 leading-none">
            {clinic.name}
          </div>
          <div class="text-brand-700/80 text-sm">{clinic.tagline}</div>
        </div>
      </div>

      <div class="flex gap-2 flex-wrap justify-end">
        <a class="chip" href="#servicos">Serviços</a>
        <a class="chip" href="#equipa">Equipa</a>
        <a class="chip" href="#contactos">Contactos</a>
      </div>
    </nav>

    <div class="mt-8 grid gap-4 md:grid-cols-2">
      <div class="card p-6 md:p-8">
        <div class="flex flex-wrap gap-2 items-center">
          <span class="chip-accent">Maia</span>
          <span class="text-brand-700/80 text-sm">Atendimento sob marcação</span
          >
        </div>

        <h1 class="mt-4 text-balance text-center">
          Cuidados especializados em <span class="text-brand-700"
            >Fisioterapia</span
          > e{" "}
          <span class="text-brand-700">Osteopatia</span>.
        </h1>

        <p class="mt-4 text-brand-700/80 max-w-2xl mx-auto text-center">
          Reabilitação, terapia manual e exercício clínico — com acompanhamento
          individualizado para o seu objetivo.
        </p>

        <div class="mt-6 flex gap-3 flex-wrap justify-center">
          <a
            class="btn-primary"
            href={whatsappLink}
            target="_blank"
            rel="noopener">Marcar por WhatsApp</a
          >
          <a class="btn-secondary" href={`tel:${clinic.phoneE164}`}>Ligar</a>
          <a class="btn-secondary" href={`mailto:${clinic.email}`}>Email</a>
        </div>

        <div class="mt-6 grid gap-4">
          <div class="card-soft p-5">
            <div class="text-brand-700/80 text-xs">Morada</div>
            <div class="mt-1 font-semibold text-brand-900">
              {clinic.address}
            </div>
          </div>

          <div class="grid gap-4 md:grid-cols-2">
            <div class="card-soft p-5">
              <div class="text-brand-700/80 text-xs">Telefone</div>
              <div class="mt-1 font-semibold text-brand-900">
                {clinic.phoneDisplay}
              </div>
            </div>
            <div class="card-soft p-5">
              <div class="text-brand-700/80 text-xs">Email</div>
              <div class="mt-1 font-semibold text-brand-900 break-words">
                {clinic.email}
              </div>
            </div>
          </div>
        </div>
      </div>

      <div class="card p-6 md:p-8">
        <div class="section-head">
          <h2>Horário</h2>
          <p class="subtitle">Aberto com marcação prévia.</p>
        </div>

        <div class="grid gap-3">
          {
            clinic.hours.map((h) => (
              <div class="card-soft p-5">
                <div class="font-semibold text-brand-900">{h.label}</div>
                <div class="text-brand-700/80">{h.value}</div>
              </div>
            ))
          }
        </div>

        <div class="mt-5 text-sm text-brand-700/80">
          Dica: se preferir, deixe mensagem por WhatsApp e confirmamos a
          disponibilidade.
        </div>
      </div>
    </div>
  </div>
</header>
