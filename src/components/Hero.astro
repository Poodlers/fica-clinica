---
import { clinic } from "../data/clinic";
import ficaLogo from "../assets/fica_logo.jpeg";
import { Image } from "astro:assets";

const whatsappText = encodeURIComponent(
  "Olá! Gostaria de marcar uma consulta."
);
const whatsappLink = `https://wa.me/${clinic.phoneE164.replace("+", "")}?text=${whatsappText}`;
const mapsLink = clinic.mapsLink;
---

<header class="section bg-brand-wash" data-reveal>
  <div class="container">
    <div class="card p-6 md:p-10">
      <div class="grid gap-4 md:grid-cols-2 md:items-center">
        <!-- LEFT: Logo -->
        <div class="flex justify-center md:justify-start">
          <Image
            src={ficaLogo}
            alt={clinic.name}
            loading="eager"
            class="w-48 sm:w-56 md:w-64 lg:w-72 aspect-square object-contain"
          />
        </div>

        <!-- RIGHT: Content -->
        <div class="text-center md:text-left">
          <p
            class="text-brand-700/80 text-sm md:text-base max-w-xl mx-auto md:mx-0"
          >
            {
              clinic.tagline ??
                "Fisioterapia e Osteopatia — acompanhamento individualizado, sob marcação."
            }
          </p>

          <h1 class="mt-3 text-balance">
            Cuidados especializados em{" "}
            <span class="text-brand-700">Fisioterapia</span>{" "}
            e{" "}
            <span class="text-brand-700">Osteopatia</span>
          </h1>

          <p class="mt-4 text-brand-700/80 max-w-xl mx-auto md:mx-0">
            Reabilitação, terapia manual e exercício clínico — com foco no seu
            objetivo e na sua recuperação.
          </p>

          <!-- CTAs -->
          <div
            class="mt-6 flex flex-wrap gap-3 justify-center md:justify-start"
          >
            <a
              class="btn-primary"
              href={whatsappLink}
              target="_blank"
              rel="noopener"
            >
              Marcar por WhatsApp
            </a>
            <a class="btn-secondary" href={`tel:${clinic.phoneE164}`}>Ligar</a>
            <a class="btn-secondary" href={`mailto:${clinic.email}`}>Email</a>
          </div>

          <!-- Quick info -->
          <div
            class="mt-6 flex flex-wrap gap-2 justify-center md:justify-start"
          >
            <span class="chip">{clinic.phoneDisplay}</span>
            <span class="chip">
              {clinic.addressShort ?? "Castêlo da Maia, Maia"}
            </span>
            <a class="chip" href={mapsLink} target="_blank" rel="noopener">
              Abrir no Maps →
            </a>
          </div>
        </div>
      </div>

      <!-- Hours -->
      <div class="mt-8 grid gap-4 sm:grid-cols-2 md:grid-cols-3">
        <div class="card-soft p-5 text-center">
          <div
            class="text-xs font-semibold text-brand-700/80 uppercase tracking-wide"
          >
            Dias úteis
          </div>
          <div class="mt-2 font-semibold text-brand-900">8h – 20h</div>
          <div class="text-sm text-brand-700/80 mt-1">Sob marcação</div>
        </div>

        <div class="card-soft p-5 text-center">
          <div
            class="text-xs font-semibold text-brand-700/80 uppercase tracking-wide"
          >
            Sábado
          </div>
          <div class="mt-2 font-semibold text-brand-900">8h – 13h</div>
          <div class="text-sm text-brand-700/80 mt-1">Sob marcação</div>
        </div>

        <div class="card-soft p-5 text-center sm:col-span-2 md:col-span-1">
          <div
            class="text-xs font-semibold text-brand-700/80 uppercase tracking-wide"
          >
            Resposta rápida
          </div>
          <div class="mt-2 font-semibold text-brand-900">WhatsApp</div>
          <div class="text-sm text-brand-700/80 mt-1">Marcação e dúvidas</div>
        </div>
      </div>
    </div>
  </div>
</header>
