---
import { clinic } from "../data/clinic";
const year = new Date().getFullYear();
---

<footer class="mt-16 bg-brand-900 text-white border-t border-brand-700">
  <div class="container py-12">
    <!-- Top row -->
    <div class="grid gap-8 md:grid-cols-3">
      <!-- Brand / description -->
      <div>
        <div class="flex items-center gap-3">
          <!-- Logo placeholder (swap with real logo) -->
          <div
            class="bg-white/10 w-12 h-12 rounded-2xl grid place-items-center"
          >
            <span class="font-extrabold text-lg">F</span>
          </div>
          <div>
            <div class="font-extrabold leading-none">{clinic.name}</div>
            <div class="text-white/70 text-sm">{clinic.tagline}</div>
          </div>
        </div>

        <p class="mt-4 text-white/70 text-sm max-w-sm">
          Cuidados especializados em fisioterapia e osteopatia, com
          acompanhamento individualizado e foco na recuperação funcional.
        </p>
      </div>

      <!-- Contact info -->
      <div>
        <h3 class="font-semibold text-sm uppercase tracking-wide text-white/80">
          Contactos
        </h3>

        <ul class="mt-4 space-y-2 text-sm text-white/70">
          <li>{clinic.address}</li>
          <li>
            <a
              href={`tel:${clinic.phoneE164}`}
              class="text-white/70 hover:text-white"
            >
              {clinic.phoneDisplay}
            </a>
          </li>
          <li>
            <a
              href={`mailto:${clinic.email}`}
              class="text-white/70 hover:text-white"
            >
              {clinic.email}
            </a>
          </li>
        </ul>
      </div>

      <!-- Navigation -->
      <div>
        <h3 class="font-semibold text-sm uppercase tracking-wide text-white/80">
          Navegação
        </h3>

        <ul class="mt-4 space-y-2 text-sm">
          <li>
            <a href="#servicos" class="text-white/70 hover:text-white">
              Serviços
            </a>
          </li>
          <li>
            <a href="#equipa" class="text-white/70 hover:text-white">
              Equipa
            </a>
          </li>
          <li>
            <a href="#contactos" class="text-white/70 hover:text-white">
              Contactos
            </a>
          </li>
        </ul>
      </div>
    </div>

    <!-- Divider -->
    <div
      class="mt-10 border-t border-white/15 pt-6 flex flex-col md:flex-row gap-4 items-center justify-between text-sm text-white/60"
    >
      <span>© {year} {clinic.name}. Todos os direitos reservados.</span>
      <!-- 
      <div class="flex gap-4">
        <a href="#" class="hover:text-white">Política de Privacidade</a>
        <a href="#" class="hover:text-white">Termos</a>
      </div>-->
    </div>
  </div>
</footer>
